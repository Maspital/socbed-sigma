#!/usr/bin/env bash

if [ $# -eq 0 ]; then
  echo "Provide path for dataset to label. Exiting..."
  exit 1
fi

if [[ $1 == *sigma.json ]]; then
  python src/label_sigma.py "$1"
else
  echo "File must end with \"sigma.json\""
fi
